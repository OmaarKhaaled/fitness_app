import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFF4100);
  static const white = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFD3D3D3);
  static const hint = Color(0xFFD9D9D9);
  static const transparent = Colors.transparent;
  static const black = Color(0xFF000000);
  static const redAccent = Colors.redAccent;
  static const green = Colors.green;
  //================ON BOARDING COLORS================
  static final onboardingCardBackground = const Color(
    0xFF242424,
  ).withValues(alpha: 0.1);
  static const navBarBg = Color(0xCC242424);
}
