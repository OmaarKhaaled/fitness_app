# name: Flutter CI

# on:
#   push:
#     branches: [ "main", "develop", "feature/**" ]
#   pull_request:

# concurrency:
#   group: flutter-ci-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       # ✅ Cache pub dependencies
#       - name: Cache pub
#         uses: actions/cache@v4
#         with:
#           path: ~/.pub-cache
#           key: pub-${{ runner.os }}-${{ hashFiles('**/pubspec.lock') }}
#           restore-keys: |
#             pub-${{ runner.os }}-

#       # ✅ Setup Flutter (CRITICAL FIX)
#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: 3.22.0   # ⚠️ DO NOT use "stable"
#           cache: true

#       - name: Install dependencies
#         run: flutter pub get

#       - name: Analyze
#         run: flutter analyze

#       - name: Run tests
#         run: flutter test